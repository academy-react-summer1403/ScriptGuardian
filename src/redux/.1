<!--     
      const user = useSelector((state) => state.user);

      const dispatch = useDispatch();
    
      console.log(user, "This Store Redux");
    
      const onChangeEmail = () => {
        dispatch(onEmailChange("example22222222222@gmail.com"));
      };
    
    
    <button onClick={onChangeEmail} style={{ display: "block" }}>
        Change email
      </button>
      {user.email} -->